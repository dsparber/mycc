<?xml version="1.0" encoding="UTF-8"?>
<ContentPage
	xmlns="http://xamarin.com/schemas/2014/forms"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:i18n="clr-namespace:resources;assembly=MyCryptos"
	xmlns:components="clr-namespace:view.components;assembly=MyCryptos"
	x:Class="view.CoinsView"
	Title="{i18n:Translate CoinsTitle}">
	<ContentPage.ToolbarItems>
		<ToolbarItem
			Text="{i18n:Translate Add}"
			Clicked="Add" />
		<ToolbarItem
			Text="{i18n:Translate Sources}"
			Clicked="SourcesClicked" />
	</ContentPage.ToolbarItems>
	<ContentPage.Content>
		<StackLayout>
			<components:HeaderView
				x:Name="Header" />
			<TableView
				Intent="Data">
				<TableSection
					x:Name="CoinsSection" />
			</TableView>
		</StackLayout>
	</ContentPage.Content>
</ContentPage>