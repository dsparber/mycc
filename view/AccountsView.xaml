<?xml version="1.0" encoding="UTF-8"?>
<ContentPage
	xmlns="http://xamarin.com/schemas/2014/forms"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:i18n="clr-namespace:resources;assembly=MyCryptos"
	xmlns:components="clr-namespace:view.components;assembly=MyCryptos"
	x:Class="view.AccountsView"
	Title="{i18n:Translate AccountsTitle}">
	<ContentPage.ToolbarItems>
		<ToolbarItem
			Text="{i18n:Translate Add}"
			Clicked="Add" />
		<ToolbarItem
			Text="{i18n:Translate Sources}"
			Clicked="SourcesClicked" />
	</ContentPage.ToolbarItems>
	<ContentPage.Content>
		<StackLayout>
			<components:HeaderView
				x:Name="Header" />
			<TableView
				Intent="Data"
				Margin="0,-8,0,0"
				x:Name="AccountsTable" />
		</StackLayout>
	</ContentPage.Content>
</ContentPage>