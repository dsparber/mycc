<?xml version="1.0" encoding="UTF-8"?>
<ContentPage
	xmlns="http://xamarin.com/schemas/2014/forms"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:i18n="clr-namespace:resources;assembly=MyCryptos"
	x:Class="view.AddCoinView"
	Title="{i18n:Translate AddAccountTitle}">
	<ContentPage.ToolbarItems>
		<ToolbarItem
			Text="{i18n:Translate Cancel}"
			Clicked="Cancel"
			Order="Primary"
			Priority="0" />
		<ToolbarItem
			Name="{i18n:Translate Save}"
			Clicked="Save"
			Order="Primary"
			Priority="1" />
	</ContentPage.ToolbarItems>
	<ContentPage.Content>
		<TableView
			Intent="Form">
			<TableView.Root>
				<TableSection
					Title="{i18n:Translate Label}">
					<EntryCell
						x:Name="LabelInput"
						Label="{i18n:Translate Label}"
						Placeholder="{i18n:Translate LabelExample}" />
				</TableSection>
				<TableSection
					Title="{i18n:Translate Coins}">
					<EntryCell
						x:Name="ValueInput"
						Label="{i18n:Translate Value}"
						Placeholder="{i18n:Translate ValueExample}" />
					<EntryCell
						x:Name="CurrencyInput"
						Label="{i18n:Translate Currency}"
						Placeholder="{i18n:Translate CurrencyExample}" />
				</TableSection>
			</TableView.Root>
		</TableView>
	</ContentPage.Content>
</ContentPage>