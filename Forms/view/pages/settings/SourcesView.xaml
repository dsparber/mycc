<?xml version="1.0" encoding="UTF-8"?>
<ContentPage
	xmlns="http://xamarin.com/schemas/2014/forms"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:i18N="clr-namespace:MyCryptos.Forms.Resources;assembly=MyCC.Forms"
	xmlns:components="clr-namespace:MyCryptos.view.components;assembly=MyCC.Forms"
	xmlns:constants="clr-namespace:constants;assembly=MyCC.Forms"
	BackgroundColor="{x:Static constants:AppConstants.BackgroundColor}"
	x:Class="MyCryptos.Forms.view.pages.SourcesView"
	Title="{i18N:Translate Sources}">
	<ContentPage.ToolbarItems>
		<ToolbarItem
			Text="{i18N:Translate Add}"
			Clicked="Add" />
	</ContentPage.ToolbarItems>
	<ContentPage.Content>
		<components:ChangingStackLayout>
			<components:HeaderView
				x:Name="Header" />
			<StackLayout
				VerticalOptions="FillAndExpand">
				<TableView
					Intent="Form"
					x:Name="Table"
					VerticalOptions="FillAndExpand">
					<TableView.BackgroundColor>
						<OnPlatform
							x:TypeArguments="Color"
							Android="#EEE" />
					</TableView.BackgroundColor>
					<TableSection
						x:Name="AddressSection" />
					<TableSection
						x:Name="BittrexSection" />
					<TableSection
						x:Name="ManualSection" />
				</TableView>
				<ContentView
					x:Name="NoSourcesView"
					VerticalOptions="FillAndExpand"
					BackgroundColor="{x:Static constants:AppConstants.TableBackgroundColor}">
					<StackLayout
						VerticalOptions="CenterAndExpand"
						Margin="40">
						<Label
							Text="{i18N:Translate NoSources}"
							VerticalOptions="Center"
							HorizontalTextAlignment="Center"
							TextColor="{x:Static constants:AppConstants.FontColorLight}" />
						<Label
							Text="{i18N:Translate NoSourcesDescriptionAdd}"
							VerticalOptions="Center"
							HorizontalTextAlignment="Center"
							WidthRequest="150"
							TextColor="{x:Static constants:AppConstants.FontColorLight}" />
					</StackLayout>
				</ContentView>
			</StackLayout>
		</components:ChangingStackLayout>
	</ContentPage.Content>
</ContentPage>